machine:
  environment:
    GRADLE_OPTS: '-Dorg.gradle.jvmargs="-Xmx256m -XX:+HeapDumpOnOutOfMemoryError"'
    ADB_INSTALL_TIMEOUT: 8

dependencies:
  pre:
    - git clone git@github.com:vinli/build.git .build
  override:
    - .build/android/pretest.sh

test:
  override:
    - .build/android/test.sh
